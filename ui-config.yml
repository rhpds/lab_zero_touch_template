---

antora:
  name: modules        # Must match 'name:' in content/antora.yml
  dir: www             # Subdirectory within the HTTP root where Antora built HTML lives
                       # (matches site.yml output.dir: ./www/www relative to ./www/)
  version: latest      # Must match version: in content/antora.yml — nookbag builds iframe src from this
  modules:
    - name: module-01  # Filename in content/modules/ROOT/pages/ without .adoc
      label: "Overview"
    - name: module-02
      label: "Lab infrastructure"
    - name: module-03
      label: "Setup automation"
    - name: module-04
      label: "Configuring the UI"
    - name: module-05
      label: "Services and routes"
    - name: module-06
      label: "Runtime automation"
      solveButton: true  # Show a Solve button — validation-host1.sh blocks if task incomplete
    - name: module-07
      label: "Writing content"
    - name: module-08
      label: "Deploying your lab"

tabs:
  - name: ">_ terminal"
    type: "terminal"   # Auto-connects to bastion SSH via wetty (port 443, path /tty1).
                       # Requires a deployed cluster — see module-04 for local preview notes.
  - name: "AsciiDoc syntax"
    url: https://docs.asciidoctor.org/asciidoc/latest/syntax-quick-reference/
    external: true     # Opens in a new browser tab

  # Module-conditional tab example (commented out — remove '#' to activate):
  # - name: "AAP"
  #   url: "https://control-${guid}.${domain}/"
  #   external: false         # embedded iframe; route must delete X-Frame-Options
  #   modules:
  #     - module-03           # only show in module-03, module-04, module-05
  #     - module-04           # (hidden in all other modules)
  #     - module-05           # see module-05 for the full explanation
